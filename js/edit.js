var pageUrl=window.location.pathname;
pageUrl=pageUrl.substring(pageUrl.lastIndexOf('/')+1,pageUrl.length);
if(pageUrl=='editQuestion.html'){}
if(pageUrl=='editNew.html'){}
if(pageUrl=='editThread.html'){}
if(pageUrl=='editDoc.html'){}
if(pageUrl=='editVoteThread.html'){}